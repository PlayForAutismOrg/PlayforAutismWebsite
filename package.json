{
  "name": "play-for-autism-website",
  "private": true,
  "version": "1.0.0",
  "description": "Static charity website with Square redirect checkout",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
